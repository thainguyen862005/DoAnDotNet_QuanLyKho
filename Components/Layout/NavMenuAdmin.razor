@inject NavigationManager Navigation

<nav class="nav flex-column p-3 sidebar sticky-top">

    <h5 class="text-center mb-4 border-bottom pb-2">ADMIN</h5>

    <div class="flex-grow-1 overflow-y-auto mb-3 custom-scrollbar">
        
        <NavLink class="nav-link sidebar-link" href="/admin/dashboard" Match="NavLinkMatch.All">
            📊 Dashboard
        </NavLink>

        <NavLink class="nav-link sidebar-link" href="/admin/products">
            📦 Quản lý sản phẩm
        </NavLink>

        <NavLink class="nav-link sidebar-link" href="/admin/import">
            ➕ Nhập kho
        </NavLink>

        <NavLink class="nav-link sidebar-link" href="/admin/sales-admin">
            🧾 Bán hàng
        </NavLink>

    <NavLink class="nav-link sidebar-link" href="/admin/user-management">
        📋 Quản lý người dùng
    </NavLink>

    <NavLink class="nav-link sidebar-link" href="/admin/stock-check">
        ✅ Kiểm kê kho
    </NavLink>

     <NavLink class="nav-link sidebar-link" href="/admin/history">  
        🕒 Lịch sử 
    </NavLink>
    
    <!-- LOGOUT -->
    <div class="mt-auto">
        <hr class="text-secondary">

        <NavLink href="/login" class="logout-btn">
            🚪 Đăng xuất
        </NavLink>
    </div>

</nav>

<style>
    /* SIDEBAR */
    .sidebar {
        background-color: #212529;
        width: 250px;       /* Nên cố định chiều rộng */
        height: 100vh;      /* Chiều cao bằng đúng màn hình */
        top: 0;             /* Dính sát mép trên */
        overflow: hidden;   /* Ẩn thanh cuộn của chính sidebar */
    }

    .text-center {
        text-align: center !important;
        color: white;
    }

    /* Tùy chỉnh thanh cuộn cho đẹp (ẩn thanh cuộn thô kệch mặc định) */
    .custom-scrollbar::-webkit-scrollbar {
        width: 4px;
    }
    .custom-scrollbar::-webkit-scrollbar-track {
        background: rgba(255,255,255,0.05); 
    }
    .custom-scrollbar::-webkit-scrollbar-thumb {
        background: #6c757d; 
        border-radius: 4px;
    }

    /* LINKS */
    .sidebar-link {
        color: #dee2e6;
        border-radius: 6px;
        margin-bottom: 6px;
        padding: 10px 12px;
        transition: all 0.2s ease;
        display: block; /* Đảm bảo link chiếm full dòng */
    }

    .sidebar-link:hover {
        background-color: rgba(255, 255, 255, 0.15);
        color: #fff;
    }

    .sidebar-link.active {
        background-color: #0d6efd;
        color: #fff;
        font-weight: 500;
    }

    .sidebar-link:focus {
        outline: none;
        box-shadow: 0 0 0 2px rgba(13, 110, 253, 0.5);
    }

    /* LOGOUT BUTTON */
    .logout-btn {
        display: block;
        width: 100%;
        padding: 12px 16px;
        /* margin-top: 12px; -> Không cần nữa vì đã có mt-auto ở div cha */
        text-align: center;
        font-weight: 500;
        border-radius: 8px;
        text-decoration: none;
        color: #dc3545;
        border: 1px solid #dc3545;
        background-color: transparent;
        cursor: pointer;
        transition: all 0.2s ease;
    }

    .logout-btn:hover {
        background-color: #dc3545;
        color: #fff;
    }

    .logout-btn:focus-visible {
        outline: none;
        box-shadow: 0 0 0 3px rgba(220, 53, 69, 0.4);
    }

    .logout-btn:active {
        transform: scale(0.97);
    }
</style>




@code {
    void Logout()
    {
        Navigation.NavigateTo("/login", true);
    }
}