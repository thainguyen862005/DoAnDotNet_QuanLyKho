@inject NavigationManager Navigation

<nav class="sidebar d-flex flex-column">
    <div class="p-3 border-bottom border-secondary">
        <h5 class="text-center mb-0">ADMIN</h5>
    </div>

    <div class="flex-grow-1 overflow-y-auto custom-scrollbar p-3">
        <NavLink class="nav-link sidebar-link" href="/admin/dashboard" Match="NavLinkMatch.All">
            📊 Dashboard
        </NavLink>
        <NavLink class="nav-link sidebar-link" href="/admin/products">
            📦 Quản lý sản phẩm
        </NavLink>
        <NavLink class="nav-link sidebar-link" href="/admin/import">
            ➕ Nhập kho 
        </NavLink>
        <NavLink class="nav-link sidebar-link" href="/admin/sales-admin">
            🧾 Bán hàng
        </NavLink>
        <NavLink class="nav-link sidebar-link" href="/admin/user-management">
            📋 Quản lý người dùng
        </NavLink>
        <NavLink class="nav-link sidebar-link" href="/admin/stock-check">
            ✅ Kiểm kê kho
        </NavLink>
        <NavLink class="nav-link sidebar-link" href="/admin/history">  
            🕒 Lịch sử 
        </NavLink>
    </div>
    
    <div class="p-3 mt-auto border-top border-secondary">
        <NavLink href="/login" class="logout-btn-custom">
            <span class="me-2">🚪</span> Đăng xuất
        </NavLink>
    </div>
</nav>

<style>
    .sidebar {
        position: fixed;    /* Ghim cố định vào khung hình */
        top: 0;
        left: 0;
        bottom: 0;
        width: 250px;
        background-color: #212529;
        z-index: 1000;
        height: 100vh;      /* Full chiều cao màn hình */
    }

    /* Nút Đăng xuất theo ảnh mẫu */
    .logout-btn-custom {
        display: flex;
        align-items: center;
        justify-content: center;
        width: 100%;
        padding: 12px;
        background-color: transparent; 
        color: #dc3545;               /* Màu đỏ danger */
        border: 1px solid #dc3545;    /* Viền đỏ */
        border-radius: 8px;           /* Bo góc nhẹ */
        text-decoration: none;
        font-weight: 500;
        transition: all 0.2s ease;
    }

    .logout-btn-custom:hover {
        background-color: rgba(220, 53, 69, 0.1);
        border-color: #ff4d5e;
        color: #ff4d5e;
    }

    /* Tùy chỉnh thanh cuộn cho phần menu nội bộ */
    .custom-scrollbar::-webkit-scrollbar {
        width: 4px;
    }
    .custom-scrollbar::-webkit-scrollbar-thumb {
        background: #495057; 
        border-radius: 10px;
    }

    /* Style cho link menu */
    .sidebar-link {
        color: #dee2e6;
        text-decoration: none;
        padding: 10px 15px;
        display: block;
        margin-bottom: 5px;
        border-radius: 6px;
    }
    .sidebar-link.active {
        background-color: #0d6efd;
        color: white;
    }
</style>




@code {
    void Logout()
    {
        Navigation.NavigateTo("/login", true);
    }
}