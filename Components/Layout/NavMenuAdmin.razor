@inject NavigationManager Navigation

<nav class="d-flex flex-column vh-100 sidebar sticky-top">

    <div class="pt-3 px-3">
        <h5 class="text-center mb-4 border-bottom pb-2">ADMIN</h5>
    </div>

    <div class="flex-grow-1 overflow-y-auto px-3 custom-scrollbar">

        <NavLink class="nav-link sidebar-link" href="/admin/dashboard" Match="NavLinkMatch.All">
            📊 Dashboard
        </NavLink>

        <NavLink class="nav-link sidebar-link" href="/admin/products">
            📦 Quản lý sản phẩm
        </NavLink>

        <NavLink class="nav-link sidebar-link" href="/admin/import">
            ➕ Nhập kho
        </NavLink>

        <NavLink class="nav-link sidebar-link" href="admin/sales">
            ➖ Bán hàng
        </NavLink>

        <NavLink class="nav-link sidebar-link" href="/admin/users">
            📋 Quản lý người dùng
        </NavLink>

    </div>

    <div class="p-3 mt-auto bg-dark">
        <hr class="text-secondary mt-0">
        <button class="logout-btn w-100" @onclick="Logout">
            🚪 Đăng xuất
        </button>
    </div>

</nav>

<style>
    /* SIDEBAR */
    .sidebar {
        background-color: #212529;
        /* Đảm bảo sidebar chiếm full chiều cao viewport */
        height: 100vh !important;
    }

    .text-center {
        text-align: center !important;
        color: white;
    }

    /* Tùy chỉnh thanh cuộn cho đẹp (Webkit browsers: Chrome, Edge...) */
    .custom-scrollbar::-webkit-scrollbar {
        width: 6px;
    }

    .custom-scrollbar::-webkit-scrollbar-track {
        background: #212529;
    }

    .custom-scrollbar::-webkit-scrollbar-thumb {
        background: #495057;
        border-radius: 3px;
    }

        .custom-scrollbar::-webkit-scrollbar-thumb:hover {
            background: #6c757d;
        }

    .sidebar-link {
        color: #dee2e6;
        border-radius: 6px;
        margin-bottom: 6px;
        padding: 10px 12px;
        transition: all 0.2s ease;
        display: block;
    }

        .sidebar-link:hover {
            background-color: rgba(255, 255, 255, 0.15);
            color: #fff;
        }

        .sidebar-link.active {
            background-color: #0d6efd;
            color: #fff;
            font-weight: 500;
        }

    /* LOGOUT BUTTON */
    .logout-btn {
        display: block;
        width: 100%;
        padding: 12px 16px;
        text-align: center;
        font-weight: 500;
        border-radius: 8px;
        text-decoration: none;
        color: #dc3545;
        border: 1px solid #dc3545;
        background-color: transparent;
        cursor: pointer;
        transition: all 0.2s ease;
    }

        .logout-btn:hover {
            background-color: #dc3545;
            color: #fff;
        }
</style>

@code {
    void Logout()
    {
        Navigation.NavigateTo("/login", true);
    }
}